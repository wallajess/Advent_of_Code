package main

func checkUpdateOrder(orderingRules [][]int, updatePages [][]int) int {

	for i, row := range updatePages {
		for j, pageNum := range row {
			for k, num := range orderingRules {
				if pageNum == num[0] {
					
				}
			}
		}
	}
}
